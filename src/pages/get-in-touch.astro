---
import Stub from "../components/Stub.astro";
import { B } from "../data/nav";
---

<Stub
  title="Get in Touch"
  crumb="Solutions › CSTL › Get in Touch"
  url={`${B}get-in-touch/`}
  status="build"
  active="/get-in-touch"
  intro="The persistent sub-nav CTA. Every page also carries the inline form block, so decide whether this is a standalone page or an anchor to the form."
  items={[["Get in Touch", "…/get-in-touch/"]]}
  note="Open question: standalone page or anchor jump? A standalone page splits form conversions across two URLs in reporting."
/>
